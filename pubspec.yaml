name: capstone
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
    
  # UI & Image
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  image: ^4.0.17
  # AI Detection - Versi GitHub yang fix
  tflite_flutter:
    git:
      url: https://github.com/tensorflow/flutter-tflite.git
      ref: main
  shared_preferences: ^2.5.4

  # ➕ HAPUS/HILANGKAN yang tidak dipakai sementara:
  # flutter_vision: ^2.0.0
  # http: ^1.1.0
  # firebase_core: ^4.2.1
  # shared_preferences: ^2.1.0
  # url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_temantukang.png
    - assets/images/panduankiri.jpeg
    - assets/images/panduankanan.jpeg
    - assets/models/best.tflite
    - assets/models/labels.txt